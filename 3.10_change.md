# 2026年3月10日 关键修改记录

## 问题概述
1. referee_simulation 与 behavior 话题不匹配
2. behavior 读取不到 blackboard 中的消息
3. Ctrl+C 无法退出
4. tick 频率过高导致卡顿
5. StatefulActionNode 卡住不继续执行

---

## 修改 1: Referee Simulation 话题对齐

### 问题
simulation 发布的话题使用相对路径，behavior 订阅的是绝对路径，导致无法通信。

### 修改文件
`src/referee_simulation/src/referee_simulation_node.cpp`

### 修改内容
所有发布器话题名称添加前导 `/`：
```cpp
// 修改前
game_status_pub_ = create_publisher<...>("referee/game_status", 10);

// 修改后
game_status_pub_ = create_publisher<...>("/referee/game_status", 10);
```

### 影响的话题
- `/referee/game_status`
- `/referee/all_robot_hp`
- `/referee/event_data`
- `/referee/robot_status`
- `/referee/buff`
- `/referee/set_pose`
- `/referee/ground_robot_position`
- `/referee/rfid_status`
- `/referee/operating_mode`

---

## 修改 2: 条件节点逻辑修复

### 问题
消息未到达时返回 SUCCESS，导致机器人在不安全的情况下执行动作。

### 修改文件
1. `plugins/condition/is_game_status.cpp`
2. `plugins/condition/is_status_ok.cpp`
3. `plugins/condition/is_rfid_detected.cpp`

### 修改内容
```cpp
// 修改前
if (!msg) {
    return BT::NodeStatus::SUCCESS;  // ❌ 错误
}

// 修改后
if (!msg) {
    return BT::NodeStatus::FAILURE;  // ✅ 正确
}
```

### 影响
- 消息未到达时，机器人保持安全状态（停止或回补给区）
- 符合 fail-safe 原则

---

## 修改 3: Blackboard 读取方式修改（核心修复）

### 问题
使用 `getInput("key_port")` 无法读取 blackboard 中的数据，即使数据已经写入。

**根本原因**：BehaviorTree.CPP 的 port 系统中，`{@key}` 语法解析失败。

### 诊断结果
```
✓ 直接读取成功: bb->get("referee_gameStatus")
✗ Port 读取失败: getInput("key_port")
```

### 解决方案
**绕过 port 系统，直接从 blackboard 读取**

### 修改文件
1. `plugins/condition/is_game_status.cpp`
2. `plugins/condition/is_status_ok.cpp`

### 修改内容

**修改前（使用 getInput）**：
```cpp
auto msg = getInput<referee_interfaces::msg::GameStatus>("key_port");
if (!msg) {
    return BT::NodeStatus::FAILURE;
}
```

**修改后（直接读取 blackboard）**：
```cpp
auto bb = config().blackboard;
if (!bb) {
    return BT::NodeStatus::FAILURE;
}

referee_interfaces::msg::GameStatus msg;
try {
    msg = bb->get<referee_interfaces::msg::GameStatus>("referee_gameStatus");
} catch (const std::exception& e) {
    RCLCPP_ERROR(logger_, "Failed to read: %s", e.what());
    return BT::NodeStatus::FAILURE;
}
```

### 为什么这样修改
1. `{@key}` 语法在某些情况下无法正确解析
2. 直接使用 `bb->get("key_name")` 可以绕过 port 系统
3. 数据确实在 blackboard 中，只是 port 映射有问题

---

## 修改 4: 性能优化

### 问题
tick 频率 100 Hz 过高，导致系统卡顿。

### 修改文件
`params/sentry_behavior.yaml`

### 修改内容
```yaml
# 修改前
tick_frequency: 100  # 100 Hz

# 修改后
tick_frequency: 10   # 10 Hz
```

### 影响
- 降低 CPU 占用
- 减少卡顿
- 对行为树执行影响很小（10 Hz 已足够）

---

## 修改 5: QoS 设置优化

### 问题
默认 QoS 可能导致消息丢失。

### 修改文件
`src/qdu2026_sentry_behavior_server.cpp`

### 修改内容
```cpp
// 添加 Reliable QoS
auto referee_qos = rclcpp::QoS(10).reliable().durability_volatile();

subscribe<referee_interfaces::msg::GameStatus>(
    "/referee/game_status", "referee_gameStatus", referee_qos);
```

### 影响
- 确保裁判系统消息不丢失
- 提高通信可靠性

---

## 修改 6: Ctrl+C 退出修复

### 问题
使用 MultiThreadedExecutor 导致 Ctrl+C 无法正常退出。

### 修改文件
`src/qdu2026_sentry_behavior_server.cpp`

### 修改内容
```cpp
// 修改前
rclcpp::executors::MultiThreadedExecutor exec(...);
exec.spin();

// 修改后
rclcpp::executors::SingleThreadedExecutor exec;
exec.spin();
```

### 影响
- Ctrl+C 可以正常退出
- 简化了执行器逻辑

---

## 修改 7: StatefulActionNode 卡住问题（关键修复）

### 问题
所有继承自 `RosTopicPubStatefulActionNode` 的节点会卡住，不继续执行后续节点。

**根本原因**：StatefulActionNode 默认行为是返回 RUNNING，等待某个条件满足才返回 SUCCESS。

### 受影响的节点
1. `ChangePose`
2. `PublishSpinSpeed`
3. `PublishTwist`

### 解决方案
实现 `onStart()` 方法，让节点立即返回 SUCCESS。

### 修改文件
1. `include/qdu2026_sentry_behavior/plugins/action/change_pose.hpp`
2. `include/qdu2026_sentry_behavior/plugins/action/pub_spin_speed.hpp`
3. `include/qdu2026_sentry_behavior/plugins/action/pub_twist.hpp`

### 修改内容
在每个类中添加：
```cpp
// 立即完成，不等待
BT::NodeStatus onStart() override {
    return BT::NodeStatus::SUCCESS;
}

BT::NodeStatus onRunning() override {
    return BT::NodeStatus::SUCCESS;
}

void onHalted() override {}
```

### 为什么需要这样修改

#### StatefulActionNode 的默认行为
1. 第一次 tick：调用 `onStart()`，如果返回 RUNNING，节点保持 RUNNING 状态
2. 后续 tick：调用 `onRunning()`，直到返回 SUCCESS 或 FAILURE
3. 如果一直返回 RUNNING，节点会一直卡住

#### 我们的需求
这些节点只需要：
1. 发布一次消息
2. 立即完成
3. 不需要等待任何反馈

#### 解决方案
重写 `onStart()` 返回 SUCCESS，让节点立即完成。

---

## 修改 8: 调试日志优化

### 添加的日志
1. 订阅回调中打印接收到的消息
2. 条件节点中打印读取状态
3. 使用 ✓ 和 ✗ 符号便于识别

### 示例
```cpp
RCLCPP_INFO(logger_, "✓ Received GameStatus: progress=%d, time=%d", ...);
RCLCPP_ERROR(logger_, "✗ Failed to read from blackboard");
```

---

## 编译命令

### 推荐使用
```bash
colcon build --packages-select qdu2026_sentry_behavior referee_simulation --symlink-install
```

### --symlink-install 的好处
- XML、YAML 等资源文件修改后不需要重新编译
- 加快开发迭代速度
- C++ 代码修改后仍需重新编译

---

## 测试验证

### 启动命令
```bash
# 终端 1
ros2 launch referee_simulation referee_simulation.launch.py

# 终端 2
ros2 launch qdu2026_sentry_behavior qdu2026_sentry_behavior_launch.py
```

### 预期日志
```
✓ Received GameStatus: progress=4, time=xxx, writing to key='referee_gameStatus'
✓ Received RobotStatus: hp=400, writing to key='referee_robotStatus'
✓ GameStatus: progress=4, time=xxx
✓ RobotStatus: hp=400
✓ 已切换到 巡逻姿态 (Patrol) (模式: 1)
[IsStatusOK SIMPLE] RESULT: SUCCESS
```

---

## 核心经验教训

### 1. BehaviorTree.CPP 的 Blackboard 访问
- `{@key}` 语法在某些情况下不可靠
- 直接使用 `bb->get("key")` 更稳定
- 适用于单树结构，多树结构需要考虑作用域

### 2. StatefulActionNode 的使用
- 默认行为是等待状态变化
- 如果只需要发布消息，必须重写 `onStart()` 返回 SUCCESS
- 否则会一直卡在 RUNNING 状态

### 3. ROS 2 话题命名
- 绝对路径（`/topic`）vs 相对路径（`topic`）
- 必须保持一致，否则无法通信

### 4. 调试技巧
- 添加详细日志确认数据流
- 使用符号（✓ ✗）便于快速识别问题
- 直接读取 blackboard 验证数据是否存在

---

## 文件清单

### 修改的文件
1. `src/referee_simulation/src/referee_simulation_node.cpp`
2. `src/qdu2026_sentry_behavior/src/qdu2026_sentry_behavior_server.cpp`
3. `src/qdu2026_sentry_behavior/plugins/condition/is_game_status.cpp`
4. `src/qdu2026_sentry_behavior/plugins/condition/is_status_ok.cpp`
5. `src/qdu2026_sentry_behavior/plugins/condition/is_rfid_detected.cpp`
6. `src/qdu2026_sentry_behavior/include/qdu2026_sentry_behavior/plugins/action/change_pose.hpp`
7. `src/qdu2026_sentry_behavior/include/qdu2026_sentry_behavior/plugins/action/pub_spin_speed.hpp`
8. `src/qdu2026_sentry_behavior/include/qdu2026_sentry_behavior/plugins/action/pub_twist.hpp`
9. `src/qdu2026_sentry_behavior/params/sentry_behavior.yaml`
10. `src/qdu2026_sentry_behavior/behavior_trees/patrol.xml`

### 新增的文件
1. `src/referee_simulation/launch/referee_simulation.launch.py`
2. `test_simulation_topics.sh`
3. `diagnose_topics.sh`
4. 多个 .md 文档（分析和说明）

---

## 总结

今天的修改解决了以下核心问题：
1. ✅ 话题通信对齐
2. ✅ Blackboard 数据读取
3. ✅ 条件节点逻辑修复
4. ✅ StatefulActionNode 卡住问题
5. ✅ 性能优化
6. ✅ Ctrl+C 退出

**最关键的两个修复**：
1. **直接从 blackboard 读取数据**，绕过 port 系统
2. **重写 StatefulActionNode 的 onStart() 方法**，让节点立即完成

现在 behavior 系统应该可以正常运行了！
